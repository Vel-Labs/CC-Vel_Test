# welcome-me provenance

Source: registry skill fixture.

The upstream raw file is intentionally preserved as a fixture at:

`fixtures/upstream/welcome-me.raw.md`

The active `.skills/welcome-me/SKILL.md` is normalized into standard Agent Skills frontmatter so the onboarding prompt works without requiring a repair step before normal usage.
